[project]
name = "dab-detector"
version = "0.1.0"
description = "Webcam dab detector with MLG visual/audio effects."
requires-python = ">=3.10,<3.13"
dependencies = [
    "mediapipe>=0.10",
    "opencv-python>=4.8",
    "pygame>=2.0",
    "Pillow>=10.0",
    "numpy>=1.24",
]
